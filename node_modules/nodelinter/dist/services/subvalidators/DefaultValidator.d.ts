import ts from "typescript";
export declare class DefaultValidator implements SubValidator {
    static lintArea: "default";
    logs: Log[];
    log: LogFunction;
    run(node: ts.Node): Log[];
    private validateDefaultExists;
    private validateSimplifyDefault;
    /**
     * Generate a function that validates if the value for a `default` conforms to the param `type`.
     *
     * Not applicable for default values that must conform to `displayName`, e.g. `default: true` for
     * `displayName: 'Simplify Response'`.
     */
    private defaultValidatorGenerator;
    private validateStringDefault;
    private validateNumberDefault;
    private validateBooleanDefault;
    private validateCollectionDefault;
    private validateMultiOptionsDefault;
    private validateOptionsDefault;
}
